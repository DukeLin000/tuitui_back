services:
  postgres:
    image: 'postgres:16' # 建議鎖定版本
    container_name: tuitui_postgres
    environment:
      - 'POSTGRES_DB=tuitui_db'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: 'redis:latest'
    container_name: tuitui_redis
    ports:
      - '6379:6379'
    restart: always

  # 👇 新增這個區塊 👇
  rabbitmq:
    image: 'rabbitmq:3-management'
    container_name: tuitui_rabbitmq
    ports:
      - '5672:5672'   # 應用程式連線用
      - '15672:15672' # 管理介面 (http://localhost:15672)
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    restart: always

volumes:
  postgres_data: